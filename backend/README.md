# DiscoBears Backend

Backend service for scraping festival data and uploading to Firebase Firestore.

## Setup

1. Place your Firebase service account key at `firebase-service-account.json`
2. Create `.env` file with configuration
3. Run with Docker Compose from the project root
