# DiscoBears Frontend

React frontend with Leaflet map displaying festivals from Firebase Firestore.

## Setup

1. Create `.env.local` with Firebase configuration
2. Run with Docker Compose from the project root
